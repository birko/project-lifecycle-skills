---
id: FEATURE-001
created: 2026-09-18
---

# Task worktrees — run a task in its own checkout — Decisions

> The decision ledger for stakeholders. Every idea-branch is a row with exactly one **state**. Rows are never deleted — `removed` is a state, not a deletion — so the ledger stays auditable.

## Decisions

| ID | Decision | State | Rationale | Date | By | → Tasks |
|----|----------|-------|-----------|------|----|---------|
| D1 | Treat a worktree as a first-class workspace, serving all four motives — parallel tasks/agents, a permanently clean main copy, disposable drill checkouts, side-by-side branch review | approved | All four claimed; only parallelism is impossible with a branch, but the set is what makes this a workspace concept rather than a drill helper | 2026-09-18 | František Bereň | TASK-173, TASK-179 |
| D2 | Declare it in its own `workspace: in-place \| worktree` field, leaving `integration:` untouched | approved | `integration:` answers *how work lands*, this answers *where work happens*; one field carrying both would hand `close` a value it cannot act on, and `single-branch` + worktree is incoherent | 2026-09-18 | František Bereň | TASK-173 |
| D3 | When the running tool cannot enter a worktree: create nothing, cut `task/TASK-NNN` in place as today, and report the degradation | approved | A branch checked out in a worktree cannot be checked out in the main copy, so creating one nobody enters would silently land the work on the default branch — a trap, not a soft fallback | 2026-09-18 | František Bereň | TASK-175 |
| D4 | Prove the move from evidence (`git rev-parse --show-toplevel` equals the worktree path), never from the agent's own claim; on mismatch remove the worktree and fall back to D3 | approved | An agent asked "can you do X?" tends to answer yes — DRILL-109 measured two cold runners inventing a blessing nobody gave | 2026-09-18 | František Bereň | TASK-175 |
| D5 | Worktrees live **outside** the repository, under a declared `worktree-root:` | approved | Requester's call: an in-repo path risks polluting the repository by accident. Nothing in a repo determines where worktrees belong, so it is a declaration rather than a convention | 2026-09-18 | František Bereň | TASK-173, TASK-174 |
| D5a | Worktrees inside the repository at a fixed `<repo>/wt/` needing no declaration — **superseded by D5** | removed | Rejected on pollution risk. Kept as a row so the ledger records the choice rather than losing it | 2026-09-18 | František Bereň | — |
| D6 | `worktree-root:` absent → ask, suggesting `../wt`. No answer → write nothing, fall back to D3, report `worktree-root undeclared` | approved | Requester's words: "if no path is declared ask for one first or suggest one". The answer-less path is the rulebook's ask-step rule — a suggestion must never become a declaration nobody made | 2026-09-18 | František Bereň | TASK-174 |
| D7 | `close` commits `done` in the worktree, **leaves it (proved), then merges in the main copy** (`git merge --no-ff task/TASK-NNN`); main copy dirty or off the default branch ⇒ failed close. A session that cannot leave (pinned at launch, and so not isolated) merges from where it stands with `git -C <main>` | changed | Originally *drive the main copy from the worktree* (`git -C <main> merge`), to keep `close` one flow. **Changed 2026-09-24 by the TASK-179 trial:** inside an `EnterWorktree` session Claude Code refuses every `git -C <main copy>`, a safety guard not to be circumvented, so the original could not run in the one runtime where `pick` genuinely enters a worktree. Keeping D7 by entering with a plain directory change was rejected: in Claude Code that does not stick outside the project, so pick would nearly always fall back | 2026-09-24 | František Bereň | TASK-176 |
| D8 | Fixed close tail: **leave the worktree → merge → `git worktree remove` → `git branch -d`**; a dirty worktree at removal is reported, never force-removed | changed | Forced by git: you cannot delete the folder you are standing in, and a branch cannot be deleted while a worktree holds it. Originally merge → leave → remove → delete; **the leave moved first with D7's change** (2026-09-24, TASK-179 trial), since the merge now happens in the main copy | 2026-09-24 | František Bereň | TASK-176 |
| D9 | Ship the new declaration through both front doors in the same change — [[new-project]] creates it, [[adopt-project]] backfills it — per the layer-parity rule | approved | The rulebook's hard rule; extending one door without the other strands every project already using the skills | 2026-09-18 | František Bereň | TASK-177 |
| D10 | Give drills a declared home under the same root, replacing the current "a clone or worktree" prose with a path and a cleanup rule | approved | The same declaration doing a second job for free, and the current prose is genuinely incomplete — it names no location and no owner for deleting the copy | 2026-09-18 | František Bereň | TASK-178 |
| D11 | `integration: single-branch` with `workspace: worktree` → work in place and print one fixed line on every `pick` that reaches the branch step, saying the worktree setting has no effect and why; neither field is rewritten, nothing is asked, nothing is cached | approved | Both are declarations, so a verb reads them and never re-decides them; the effective workspace is derived from two values the repo still holds, so it is recomputed every run and starts working the moment `integration:` changes. Refusing at init misses hand edits; asking at `pick` re-decides a declaration; silence is indistinguishable from a working setup | 2026-09-24 | František Bereň | TASK-173, TASK-174 |
| D12 | `workspace:` and `worktree-root:` are two top-level lines after `integration:`; a relative root resolves against the repository root (as `siblings.root` does); absent `workspace:` means in-place, absent `worktree-root:` means undeclared | approved | The root also serves drill copies when tasks work in place (D10), so nesting it under `workspace:` would tie it to a value it does not depend on, and would turn D2's single value into a block | 2026-09-24 | František Bereň | TASK-173 |
| D13 | A task's worktree is `<worktree-root>/<repo-name>-TASK-NNN` — one flat level, the repo name taken fresh from the main copy's folder each run | approved | Task numbers repeat across sibling repos (343 ids in more than one tree, measured), so the repo name must be in the path; flat, so no checkout folder is merely `TASK-NNN` (tools that name a project after its folder collide) and no `<repo>/…/<repo>` shape re-creates the path-stripping bug `skills-lint.sh`'s `check_root` records | 2026-09-24 | František Bereň | TASK-174 |
| D14 | Every runtime *tries* to enter the worktree (its own worktree-entry tool when it has one, else a directory change), then proves it from a separate later command; there is no table of which runtimes can move | approved | Nothing can know in advance whether a move will stick without trusting the agent's claim, which D4 forbids. Measured 2026-09-24: from the main Claude Code session, the built-in worktree tool entered an outside-repo worktree and `--show-toplevel` confirmed it from both shells; a subagent's pinned directory cannot, and the proof catches that | 2026-09-24 | František Bereň | TASK-175 |
| D15 | An answer to "where should worktrees live?" is written as a live `worktree-root:` line in `tasks/.config.yml` and committed on the default branch *before* the worktree is made — inside the pick commit (D17), never as a commit of its own; the question says so up front, so there is no second confirmation | approved | Keeps the main copy clean (D1, and D7's clean check at close) and makes the answer reach every later pick. Known cost, accepted: on a repo whose default branch is protected, pushing that one-line commit may be refused | 2026-09-24 | František Bereň | TASK-174 |
| D16 | Resuming a task in a new session re-enters the worktree that holds its branch, rather than working in the main copy | approved | Found while planning TASK-174/175: a fresh session starts in the main copy, which cannot check out a branch a worktree holds, and [[fix-next]] is built to be resumed. Stakeholder asked for it to be its own task | 2026-09-24 | František Bereň | TASK-181 |
| D17 | Under `workspace: worktree`, `pick` commits the pick itself — status `in-progress`, the dashboard, a plan it just wrote, a task file not yet committed, and an answered `worktree-root:` — on the default branch, alone, before creating the worktree; the worktree then branches from that commit | approved | Written only on the task branch, the task still read `todo` from the main copy, so `/tasks`, `/fix-next` and a second pick would treat it as free (the parallelism D1 exists for), and a task file never committed did not exist in the worktree at all; written uncommitted in the main copy, it would fail D7's clean check at close. Same accepted cost as D15: one small commit on the default branch per pick | 2026-09-24 | František Bereň | TASK-174 |
| D18 | `close` works out where it runs **before writing anything** (step 4b): a linked worktree is `--git-dir` ≠ `--git-common-dir`, the main copy is the first entry of `git worktree list --porcelain`. Of the main-copy checks, the default branch and the remote are read there from any tree; **the clean check runs there only where the runtime allows it**, and otherwise right after leaving (step 8) | changed | A failed close must leave the worktree and branch untouched, and steps 5–7 already commit on the task branch, so detecting at the merge step is too late. The parent of the common dir is wrong under `--separate-git-dir`; git always lists the main copy first. **Changed 2026-09-24 by the TASK-179 trial:** an isolated Claude Code session refuses `git -C <main copy>`, so only the clean check had to move; a failure there is the merge-failed case, which re-closing resumes | 2026-09-24 | František Bereň | TASK-176 |
| D19 | In a worktree, `close` writes `done` and commits on the task branch, and the merge carries it to the default branch; the `pr:` SHA backfill rides in the merge via `merge --no-ff --no-commit`, and the dashboard is never regenerated on the task branch | approved | Forks from the D17 pick commit, so `in-progress → done` merges cleanly. A follow-up SHA commit was already rejected by `close` step 7; a dashboard regenerated on every task branch would conflict whenever tasks run in parallel | 2026-09-24 | František Bereň | TASK-176 |
| D20 | The merge landed but the session cannot leave the worktree (or a removal fails) ⇒ the task is still `done`, and the report prints the exact outstanding commands; nothing is forced | approved | `done` means merged, and it is merged. Rolling back would mean reverting a merge that landed, over what is only housekeeping | 2026-09-24 | František Bereň | TASK-176 |
| D21 | `close` removes a worktree only when `workspace: worktree` is declared and the folder is exactly the one `pick` made (`<worktree-root>/<repo-name>-TASK-NNN`, D13); any other worktree holding the branch is merged, left in place and reported. Never `--force`, never `branch -D` | approved | Never delete what the skill did not create — a hand-made worktree may hold someone's work | 2026-09-24 | František Bereň | TASK-176 |
| D22 | Under `workspace: worktree`, `close` commits what it regenerates on the default branch after the merge (dashboard, feature rollups) as one `chore: dashboard and rollups after TASK-NNN` commit; in-place projects are unchanged | approved | The mirror of D17: left uncommitted, the main copy is dirty, so the next `pick` falls back and the next `close` fails its clean check. The subject does not lead with the id, so spec provenance is not polluted | 2026-09-24 | František Bereň | TASK-176 |
| D23 | Worktree mode covers **local merges only** for now: when the default branch tracks a remote (`<default>@{upstream}` resolves — recomputed every run), `pick` works in place and `close` refuses a worktree close, each saying why; a design for PR-based projects is its own task | approved | The D17 pick commit and D22 refresh commit land on a local default branch nobody pushes, which diverges from the remote as soon as a PR merges there (every later `pull --ff-only` fails) and can never be pushed to a protected branch. Whether a branch tracks a remote is determined by evidence the repo holds, so it is recomputed, not declared. Pushing those commits was rejected (a protected branch refuses them); keeping status off the default branch was rejected (re-opens the problem D17 fixed) | 2026-09-24 | František Bereň | TASK-174, TASK-176, TASK-183 |
| D24 | Both front doors **ask** the workspace question — [[new-project]] at intake, [[adopt-project]] in its frontier round — with one wording kept in `/tasks init`; no answer leaves `workspace:` absent (`in-place`), reported as undeclared | approved | Stakeholder's choice over the recommended opt-in ("don't ask"). Asking makes the option visible to every project; the cost is one more question per scaffold or adoption | 2026-09-24 | František Bereň | TASK-177 |

_`Date` and `By` stay `—` until `/feature decide` stamps a verdict — they record **when/who decided**, not when the row was created (creation is in the History log)._

**States:** `proposed` (fresh from grill, awaiting decision) · `approved` (build it) · `deferred` (not now — note unblock condition) · `changed` (approved but altered — record the delta) · `removed` (rejected / out of scope).

Only `approved` and `changed` rows generate tasks at `/feature decompose`. No row is terminal: a `deferred`/`removed` decision overturned by later evidence (incl. production feedback) is **reopened** by adding a *new* `proposed` row that links the superseded one — the old row is never deleted.

## History log

> Append-only. Every state change gets a dated line with the reason — this is the "why it changed", not just the current value.

- 2026-09-18 — feature created; decisions seeded as `proposed` from the grill-me interview. D5a is carried as a row rather than dropped: the requester considered the in-repo location and rejected it on pollution risk, so the ledger records the choice instead of losing it.
- 2026-09-18 — D1, D2, D3, D4, D5, D6, D7, D8, D9 proposed → approved; D5a proposed → removed (in-repo `wt/` rejected on pollution risk); D10 proposed → approved. Stamped as a batch, because each verdict was already given during the grill and re-asking a resolved question is the defect this rulebook lints for.
- 2026-09-18 — **Provenance note on the batch.** D2, D4 and D8 originated as the agent's derivation during the grill rather than the stakeholder's explicit pick — D2 from the observation that `single-branch` + worktree is incoherent, D4 from the DRILL-109 measurement, D8 from two git constraints with no alternative. They were shown before stamping and approved with the rest, but the distinction is recorded so a later reader can tell which rows carry the stakeholder's own reasoning. D10 was the one row nobody had answered; it was put separately and approved on its own.
- 2026-09-24 — Decomposed under EPIC-005 / STORY-021. D1 (approved) → TASK-173, TASK-179 · D2 → TASK-173 · D3 → TASK-175 · D4 → TASK-175 · D5 → TASK-173, TASK-174 · D6 → TASK-174 · D7 → TASK-176 · D8 → TASK-176 · D9 → TASK-177 · D10 → TASK-178. D5a (removed) generates nothing. Three choices were left open on purpose, each to come back through `/feature decide` when its task settles it: what happens when a project declares worktrees but commits straight to the main line (TASK-173), the folder layout under the worktree root (TASK-174), and whether an absent `workspace:` is a gap for the adopter to raise or simply means "work in place" (TASK-177).
- 2026-09-24 — Prototype settled as **Skipped**: there is nothing visual here, and the proof is the end-to-end drill (TASK-179). The wording of the one question this feature puts to a person is pinned verbatim in TASK-174 instead.
- 2026-09-24 — D11 and D12 proposed → approved, from TASK-173's plan. Both originated as the agent's derivation and were put to the stakeholder in plain language before stamping. D11 closes the "declares worktrees but commits straight to the main line" question the decomposition left open; D12 fixes the shape of the two settings. D11 → TASK-173 (declares it), TASK-174 (carries it out — `pick`'s single-branch path never reads `workspace:` otherwise).
- 2026-09-24 — D13, D14, D15, D16 proposed → approved, from the TASK-174/175 plan; all four originated as the agent's derivation and were put in plain language before stamping. D13 closes the folder-naming question left open at decomposition. D14 rests on a probe run the same day (a throwaway worktree outside this repo, entered and proved, then removed). D16 is new scope found in planning — resuming — and was decomposed at once → TASK-181. TASK-175's criterion "a runtime known not to persist a directory change never creates the worktree" was reworded before work began, because D14 makes "known in advance" unanswerable; the D3 outcome it protected is kept.
- 2026-09-24 — D17 proposed → approved, from TASK-174/175's correctness review: the status flip had been written inside the worktree only, invisible from the main copy and missing whenever the task file was uncommitted. D15's separate declaration commit folds into this one pick commit. Landing agreed at the same time: TASK-174, TASK-175 and TASK-176 close as one change, because today's `close` would mark a worktree task `done` without merging it.
- 2026-09-24 — D18–D22 proposed → approved, from TASK-176's plan; all five originated as the agent's derivation. D20, D21 and D22 were put individually in plain language; D18 and D19 were approved together as mechanics.
- 2026-09-24 — D23 proposed → approved, from the second correctness review of TASK-174/175/176 (the PR path diverged local and remote default branches). Also from that review: parallel worktrees can mint the same task id — filed as TASK-184 by the stakeholder's choice, while the related dashboard conflict was fixed here (no verb regenerates `tasks/README.md` inside a task worktree).
- 2026-09-24 — **D7 approved → changed, and D8 with it**, from the TASK-179 trial. In a real Claude Code session inside an `EnterWorktree` worktree, `close` step 4b's `git -C <main copy> status` was refused by the runtime's isolation guard, reads included. The stakeholder chose leave-first-then-merge over dropping `EnterWorktree`. The earlier drills had all run in `-p` sessions, which are pinned rather than isolated, so `git -C` worked there. That is why the drills passed and only the interactive trial could find this.
- 2026-09-24 — D18 approved → changed, alongside D7/D8, from the review of the trial's fix: only the main-copy **clean** check needs the main copy, so only it moves after leaving; the branch and remote checks still run before anything is written. The same review fixed resuming an unmerged close from the main copy, a parked close reaching the main copy from inside an isolated session, and an isolated session that left in only one shell.
- 2026-09-24 — D24 proposed → approved: the stakeholder chose "ask in both" over the agent's recommended opt-in. It settles the absence-semantics question the decomposition left open for TASK-177.
