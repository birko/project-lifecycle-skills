---
id: FEATURE-002
created: 2026-09-18
---

# Comment discipline — keep the source free of everything that belongs elsewhere — Decisions

> The decision ledger for stakeholders. Every idea-branch is a row with exactly one **state**. Rows are never deleted — `removed` is a state, not a deletion — so the ledger stays auditable.

## Decisions

| ID | Decision | State | Rationale | Date | By | → Tasks |
|----|----------|-------|-----------|------|----|---------|
| D1 | The rule is **necessity per line, not a line count** — a long comment is legal when every line carries something, a short one is not when it carries nothing | approved | Requester's own correction: "sometimes a comment can be longer but only if it has some necessary info and not unnecessary things". Length is a symptom, not the offence | 2026-09-18 | František Bereň | TASK-140 |
| D1a | A hard 1–3 line cap — **superseded by D1** at the requester's correction | removed | Was the opening phrasing; corrected mid-grill. Kept as a row so the ledger records the correction rather than silently shipping the later form | 2026-09-18 | František Bereň | — |
| D2 | Ship a decidable test, not the word "necessary": delete the line, ask where its content already lives — code / version history / the ticket / a decision record / **nowhere**. Only *nowhere* survives, at any length | approved | Every author already believes their own comment is necessary, so the rule needs a check two reviewers answer identically. Keeps the five destinations including the decision-record row | 2026-09-18 | František Bereň | TASK-140 |
| D3 | Keep the concrete banned instances alongside the test — changelog, QA log, rationale essay above a declaration, a 10-line block on one property/const/enum/field | approved | A test without instances gets re-invented per reader; these four are the requester's own examples | 2026-09-18 | František Bereň | TASK-140 |
| D4 | The rule lives in `skills/new-project/templates/CONVENTIONS-universal.md`, spliced verbatim into the seeded guide's § Conventions, so every newly scaffolded project receives it in the same words | approved | Requester asked for it "in our claude agents template"; that template is the rulebook auto-loaded into every task's context | 2026-09-18 | František Bereň | TASK-140, TASK-145, TASK-147 |
| D5 | Already-adopted repos are **not** backfilled — [[adopt-project]] reconciles which artifacts exist and their shape, never the prose inside a hand-written document. The command (D6) is what reaches them | approved | The adopter has never judged prose inside a hand-written document, and the first time it does becomes precedent for every later prose rule. The gap is real and closed by the command instead | 2026-09-18 | František Bereň | TASK-140 (out of scope) |
| D6 | Build a new skill that owns the check; [[verify-conventions]] does **not** grow a second copy of the rule (one owning file) | approved | The conventions lint only ever sees code somebody is currently changing; the whole point is the block written two years ago in a file nobody touches | 2026-09-18 | František Bereň | TASK-142 |
| D6a | Name it **`review-comments`** | approved | The requester's own words for it, and accurate — it reports, then fixes. Rejected `verify-comments`: in this project that prefix means report-only, and a `verify-*` that writes files empties the family name. Rejected `prune-comments`: understates the relocation half in D9 | 2026-09-18 | František Bereň | TASK-142 |
| D7 | **Three** scopes: default = the work in hand (the diff), `PATH …` = the named paths in full ignoring the diff, `--all` = the whole repository | changed | **Delta from the approved two-scope shape:** the skill's invocation block always advertised a `PATH` argument and never defined it, and two other sections — § *The only copy*'s *"a scoped run where a person is looking at one file"* and its `run scoped to act on it` — already depended on a scope that did not exist. The third scope was therefore latent in the approved design rather than new capability; defining it is what makes the only-copy rule reachable outside the diff, which is the gap TASK-143 recorded. Original rationale stands: requester's words, "would like to be able to scan the whole repo but also just the files it touched by work in the diff" | 2026-09-20 | František Bereň | TASK-142, TASK-152, TASK-155, TASK-156 |
| D8 | `/tasks close` invokes it as its **own axis**, reported side by side with standards/fidelity/correctness and never merged or reranked into one list | approved | The rulebook's existing rule on independent review axes — a comment finding and a correctness blocker are not the same quantity and must not share a ranking | 2026-09-18 | František Bereň | TASK-144 |
| D9 | Only-copy rule: a comment that fails the test but is the sole record of its content is **relocated first** (task / ADR / docs) and replaced by a one-line pointer — never deleted outright. Ask before filing anything | approved | A sweep that deletes is deleting the only copy of a rationale nobody wrote down elsewhere; recovering it later means knowing it existed, which in practice means it is gone | 2026-09-18 | František Bereň | TASK-143 |
| D10 | Record a **re-runnable measurement of all six** of this repo's scripts against the rule — a verdict per file, found by the **destination search**, never by line counts — and re-run it whenever the rule or its method changes. A file passes when **no finding is raised by both of two cold readers** | changed | **Delta from the approved form**, which read *"record that `skills-lint.sh` (288 lines, 123 comments, 42%, longest block 35 lines) is compliant, so a later sweep does not strip it"*: (1) the measurement found that file **not** compliant — its verdict was an assumption, answered by counting lines rather than running the test — and it was fixed rather than exonerated; (2) the claim covered six scripts while one was examined, so the scope became all six; (3) "clean" needed a definition that terminates, because each drill surfaces smaller items at the margin — two independent readers agreeing is what separates a finding from a taste. Original rationale stands: under a length cap the file is a pile of violations, and the record exists so a later sweep does not strip what carries real content | 2026-09-24 | František Bereň | TASK-141, TASK-151, TASK-153, TASK-154, TASK-148, TASK-157, TASK-158, TASK-162, TASK-163, TASK-164, TASK-165, TASK-166 |
| D11 | Add the rule to this repo's own `AGENTS.md` § Conventions as well, not only the consumer template | approved | "This repo eats its own cooking" is a stated rule, and shipping a convention the project exempts itself from is what AGENTS.md calls evidence the rule is wrong. Safe because D10 is stamped in the same pass | 2026-09-18 | František Bereň | TASK-141 |
| D12 | A **doc comment** (docstring, XML doc, JSDoc) is a published output rather than commentary, so it survives the "restates the signature" destination — but its **content** is judged line by line like any other comment | approved | Read literally the test deletes most docstrings, and in many stacks they are a published artifact or compiler-enforced. The carve-out is narrow on purpose: it exempts the construct from one destination, never the content from the test | 2026-09-18 | František Bereň | TASK-140 |
| D13 | The rule's two copies (seed template, this repo's `AGENTS.md`) are written between **marker lines**, and a lint check asserts they match. No pointer between them is possible — a consumer install cannot see this repo | approved | The five destinations are a list that can grow, the shape § *Defer to a shared inventory* warns about, and the only available mitigation was a convention nobody enforces. This one is machine-checkable, so it becomes a gate instead | 2026-09-18 | František Bereň | TASK-140, TASK-146 |
| D14 | When a project has recorded **no** comment rule, `review-comments` reads the shipped universal copy at runtime, prefixes every finding `universal:`, caps it at ⚠ (never a blocker), and lets any project rule override outright | approved | D5 means adopted repos are never backfilled, so "no rule recorded" is the common case, not the edge — refusing there defeats D6, which exists to reach exactly those repos. The three guards are [[verify-conventions]]' smell-baseline posture, in-house precedent rather than a new bet | 2026-09-19 | František Bereň | TASK-142 |
| D15 | The destination table gains a **sixth row — the project's own guide** — with the same action as the decision-record row: *delete it, or leave one line pointing at the section* | approved | The five rows named code, version history, the ticket, a decision record and *nowhere*, and the rule text inside the markers never mentioned a guide — yet this repo's own five-records table makes `AGENTS.md § Conventions` a **distinct** record, so guide-resident content had a home and matched no row. Measured: three readers split on it, and TASK-154's own relocation to § Testing was filed under *a decision record*, which § Testing is not. **Delete-or-pointer rather than delete-outright**, matching the decision-record row: the guide is where this repo's cross-cutting content lives by design, so a reader who loses the pointer has nothing local telling them the rule exists — which is the argument the check-6 header was rebuilt on, and a delete-outright row would indict this feature's own remedy | 2026-09-20 | František Bereň | TASK-159, TASK-160 |

_`Date` and `By` stay `—` until `/feature decide` stamps a verdict — they record **when/who decided**, not when the row was created (creation is in the History log)._

**States:** `proposed` (fresh from grill, awaiting decision) · `approved` (build it) · `deferred` (not now — note unblock condition) · `changed` (approved but altered — record the delta) · `removed` (rejected / out of scope).

Only `approved` and `changed` rows generate tasks at `/feature decompose`. No row is terminal: a `deferred`/`removed` decision overturned by later evidence (incl. production feedback) is **reopened** by adding a *new* `proposed` row that links the superseded one — the old row is never deleted.

## History log

> Append-only. Every state change gets a dated line with the reason — this is the "why it changed", not just the current value.

- 2026-09-18 — feature created; decisions seeded as `proposed` from the grill-me interview. D1a is carried as a row rather than dropped: the 1–3 line cap was the requester's own opening phrasing and was corrected mid-grill, so the ledger records the correction instead of silently shipping the later form. The command half (D6–D9) was raised as "maybe" and is stamped independently of the rule half (D1–D5) — deferring it does not block the rule.
- 2026-09-18 — D1, D3, D4, D6, D7, D8, D9, D10 proposed → approved as a batch; D1a proposed → removed. Each was already settled by the requester's own words or by a rule already in the book, and re-asking a resolved question is the defect this rulebook lints for.
- 2026-09-18 — D2 proposed → approved, five destinations kept intact. The alternative considered and rejected was collapsing the decision-record row into D9's relocation rule; rejected because the test answers *where does this belong* and D9 answers *what does the command do about it*, which are different questions that happen to name the same destination.
- 2026-09-18 — D5 proposed → approved unchanged, after being put explicitly as the weakest row. The rejected alternative was having [[adopt-project]] offer the bullet; rejected because it would be the first time the adopter edits prose rather than structure, and that becomes the precedent every later prose rule cites.
- 2026-09-18 — D6a proposed → approved as `review-comments`. `verify-comments` rejected: the `verify-*` family is report-only here and a member that writes files empties the name. `prune-comments` rejected: understates D9's relocation.
- 2026-09-18 — D11 proposed → approved. Consequence to watch: the next `/verify-conventions` run judges this repo's own scripts by the new rule, which is precisely why D10 had to be stamped in the same pass rather than later.
- 2026-09-18 — decomposed into EPIC-004 (two stories, five tasks). D1, D2, D3, D4 (approved) → TASK-140. D10, D11 → TASK-141. D6, D6a, D7 → TASK-142. D9 → TASK-143. D8 → TASK-144.
- 2026-09-18 — **D5 generated no task, deliberately.** It is an approved decision *not* to act: [[adopt-project]] does not backfill the rule into already-adopted repos. A do-nothing ticket would sit in the backlog forever and misreport how much of this epic is left, so the boundary is recorded as an explicit `## Out of scope` entry on TASK-140 instead, where anyone about to cross it will read it.
- 2026-09-18 — **D12 added and approved in one step**, during planning of TASK-140. The Plan pass found that the five-destination test, read literally, orders most docstrings deleted — `/// <summary>Gets the user id.</summary>` is content that already lives in the code. Raised to the stakeholder rather than resolved in passing, per the rule that a choice altering what the feature *says* is ledger-worthy even when an agent found it. Stakeholder's refinement is load-bearing and is in the row: the doc comment survives, **its content still gets checked** for padding and for information that belongs elsewhere. A blanket exemption was not what was asked for.
- 2026-09-18 — D4 — TASK-145 spawned from TASK-140 (patch the scaffolder's "leave these sub-blocks as-is" list, which names two sub-blocks and would silently go one short now a third exists). Covered by D4 rather than a new row: it realizes the same decision — the rule living in the seeded rulebook — by stopping the next scaffold run from overwriting it.
- 2026-09-18 — **D13 added and approved during the grill of TASK-140's plan.** The plan surfaced that D11 creates a second copy of the rule with no pointer possible between them, and proposed a verbatim-copy convention as mitigation. Rejected in favour of a lint check: this repo's stated position is that an unenforced convention is worth what an unrun check is, and unlike most prose rules this one is machine-checkable. Also rejected: dropping the rule from `AGENTS.md` to leave one copy — that reverses D11 and the eat-your-own-cooking rule with it. Consequence for TASK-140: the section must be written between markers, which is why this is decided now rather than at TASK-141.
- 2026-09-18 — **Grill of TASK-140's plan, second outcome (no new row).** The rule ships with **no line count anywhere** — not "1–3 lines", not "keep it short". Calibration is given in words. This confirms D1/D1a rather than altering them: the opening brief's *"in 1-3 lines"* was already superseded, and the grill settled the separate question of whether the number survives as a *signal*. It does not. The only numeral in the section is "ten lines above one property", present as a banned instance and immediately shown passing in a different position.
- 2026-09-18 — **D12 clarified after the cold drill; the row's shape is unchanged.** The runner hit a case the clause did not settle: a structurally required tag that adds nothing beyond the signature (`@param tolerance the tolerance`) is cuttable by "cut the ones that add nothing" and uncuttable by "a build may require it". It resolved the conflict sensibly by judgement; the rule now states the resolution instead — a required tag is **filled, not deleted**. Recorded here rather than as a new row because the decision (content is judged line by line) never changed; what changed is that the tie-break is now written down rather than re-derived per reader.
- 2026-09-19 — **D4's row rewritten to the current form; state unchanged at `approved`.** TASK-147 moved the rule from `CLAUDE.seed.md` to `templates/CONVENTIONS-universal.md`, spliced in at a token. **Not stamped `changed`, deliberately:** what was agreed — the rule reaches every scaffolded project through its seeded rulebook — is exactly what happens, and better than before, since consumers now get it word for word instead of reworded. Only the file holding it moved, which the routing table calls an implementation detail. The row is rewritten rather than annotated because a live row describing a file that no longer holds the rule is the classic lie this ledger's rules forbid.
- 2026-09-19 — **D14 added and approved**, raised by the planning pass on TASK-142 as the feature's largest open risk. Rejected: refusing to run (purest, but useless in most repos by D5's own consequence); and offering to write the rule into the project's guide first (most honest, and it would make TASK-142 an ask-step, growing TASK-144's `--unattended` contract by a row the design was shaped to avoid). The guards are not decoration — drop the label, the cap or the project-overrides rule and the command becomes the adversarial pass D5 declined to build into [[adopt-project]].
- 2026-09-20 — **D10's measurement was re-run and its verdict changed.** TASK-141 recorded the measurement D10 asked for, but answered the wrong question: it counted lines and read the two largest blocks for rationale, rather than running the rule's own destination search. `/review-comments --all` then found four real findings inside the very file D10 blessed. TASK-151 re-ran the table against the actual test and rewrote it to state which question it answered; TASK-153 was spawned from that walk for a fifth copy of the check-4 measurement in `skills-lint-test.sh`. Both appended to D10's `→ Tasks`. **The decision itself is unchanged** — "record the measurement" was and is right; what changed is the measurement's content and its verdict, which is the artifact, not the choice. D10's row text still quotes the superseded figures (288 / 123 / 42% / 35) and is a candidate for a `changed` stamp at the next `/feature decide`.
- 2026-09-20 — **TASK-154** spawned from TASK-151's human test plan: two check headers in `skills-lint.sh` restate reasoning held in `AGENTS.md` § Testing and in D13 itself. Appended to D10's `→ Tasks` — it is the same measurement finding its own residue, not new scope.
- 2026-09-20 — **D7 approved → changed: two scopes became three.** TASK-152 found that `PATH …` had been advertised in the invocation block since the skill shipped and defined nowhere, so a caller writing `/review-comments src/mill.ts` got whatever the reader improvised. Settled as a **whole-file sweep that ignores the diff and asks the only-copy question**. Stamped `changed` rather than appended to the approved row, because the row's own words were *"Two scopes"* — quietly widening it is the move the ledger rules forbid. Measured before stamping: six cold runners over three rounds resolved the scope **6 of 6** identically, the only-copy question fired **4 of 4**, and the untracked/named-generated rules held **2 of 2**. The same drill measured a **5:1 severity divergence** on an unrelated rule, filed as TASK-155 rather than folded in here.
- 2026-09-20 — **D15 raised as `proposed`** from TASK-154's verification run, and left undecided deliberately: the fix edits `CONVENTIONS-universal.md`, which ships byte-identical to every consumer and is asserted by lint check 5, so it is a decision rather than an edit. TASK-159 is **blocked** on it and blocks TASK-158's `:10` finding. Needs `/feature decide`.
- 2026-09-20 — **D15 proposed → approved**, with the action settled as *delete-or-pointer*. The alternatives were weighed rather than assumed: *delete outright* was rejected because the guide is this repo's primary record for cross-cutting rules, so stripping the pointer leaves a reader no local trace that a rule governs the line — and it would make violations of this feature's own remedy, the two pointers TASK-154 installed. *Keep five rows* was rejected because it leaves the axis blind to duplication of § Conventions, which is most of what this repo records. TASK-159 unblocks; TASK-158's `:10` finding is now judged **confirmed** on its guide half as well as its code half.
- 2026-09-20 — **Ledger reconciled at the `/feature review` gate, which is what caught it.** Five tasks carried `feature: FEATURE-002` but appeared in no `→ Tasks` cell: **TASK-147** → D4 (the verbatim splice the decision asks for), **TASK-148 / TASK-157 / TASK-158** → D10 (all descend from TASK-141's measurement — 148 spawned while it ran, 157 from finally running its test plan, 158 from 157's verification), **TASK-160** → D15 (the eight sites its new row made reportable). The deciding rules require reconciling *before* this gate; it had not been done, and the gap was invisible from the task side because every one of the five is correctly linked in its own frontmatter. Direction matters: decision→task was complete, task→decision was not.
- 2026-09-24 — D10 — **TASK-162** appended: filed 2026-09-20 from TASK-141's re-run (the first sweep of `skills-lint-test.sh` by anyone), it carried `feature: FEATURE-002` but was missing from this cell — the same task→decision gap the review-gate reconciliation above caught five of. **TASK-163** spawned from TASK-162's drill: five verified comment findings outside its nine, same measurement's residue.
- 2026-09-24 — D10 — **TASK-164** spawned from TASK-163's drill: two comment findings each raised by one cold reader of two, borderline on the rule's own terms, grouped so dismissal-with-reason stays a legitimate outcome.
- 2026-09-24 — D10 — **TASK-165** spawned from TASK-164's drill: three comments restating the line beside them, minor; one (`skills-lint.sh:81`) now raised by three readers across two drills.
- 2026-09-24 — D10 — **TASK-166** spawned to clear the three findings every reader pair agreed on before TASK-141's re-run (the `pi-install` headers, the `ARG_RE` block, the EVIDENCE/PIN lines). The last reverses TASK-153's accepted form at the stakeholder's call: each line now states what its case detects rather than what a past run printed — the same fact, no longer a log.
- 2026-09-24 — **D10 approved → changed**, stamped by the stakeholder at the reconciliation before `/feature review`. The row now describes what was built: a re-runnable, per-file measurement of all six scripts under the destination search, with *pass = no finding both of two cold readers raise* (defined the same day, on TASK-141). The approved row's figures (288 / 123 / 42% / 35) and its *compliant* verdict were both superseded by the measurement itself — the verdict first by TASK-151, the scope by TASK-141's re-run. Final state: all six scripts pass (TASK-141's third re-run, signed off); `AGENTS.md` § Comments carries the table, re-measured thirteen times. Also recorded, affecting no row: TASK-166 reversed TASK-153's accepted form of the test file's EVIDENCE/PIN lines at the stakeholder's call — each now states what its case detects rather than what a past run printed.
