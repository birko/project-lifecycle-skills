---
id: FEATURE-002
generated: 2026-09-20
---

# Comment discipline in agent-written code — Status

> Auto-generated by `/feature status`. Written for project managers / non-technical stakeholders — no code jargon. Do not hand-edit; re-run the verb.

**Phase:** building

## Decisions at a glance

| State | Count |
|-------|-------|
| ✅ approved | 14 |
| ✏️ changed | 1 |
| ⏸️ deferred | 0 |
| ❌ removed | 1 |
| 💭 proposed (undecided) | 0 |

The one changed decision is recent: the checking command was agreed with two ways of choosing what
to look at, and a third had been advertised to users since the day it shipped without ever being
explained. It has now been defined, and the original decision was re-stamped rather than quietly
widened, so the record shows the shape the requester approved and the shape that was built.

## Build progress

9 / 14 tasks done. One further task was cancelled — its work shipped inside another, and the
remaining part stopped being possible when a different change removed what it applied to.

**The whole of what was asked for is built and in use.** The rule is in the template every new
project receives, this project follows it, an automated check keeps the two copies of the wording
from drifting apart, and the command exists, runs over either a change or the whole codebase, and
is invoked automatically whenever work is closed.

The five open items are all follow-ups that the feature's own testing produced — none is part of
the original request, and none blocks anyone:

| Item | What it is |
|---|---|
| TASK-148 | Two installer scripts explain a choice that is already written down elsewhere |
| TASK-153 | A measurement is repeated in a fifth place |
| TASK-154 | Two notes in the check script argue a case the rulebook already settles |
| TASK-155 | Six independent testers read one case and one of them graded it differently |
| TASK-156 | A report line still varies slightly between testers; one rule was written but never tested |

## What can be tested now

**Four of the five open items carry a test plan ready to run** — TASK-153, TASK-154, TASK-155 and
TASK-156. Each is small and each ends by re-running the command and checking that a specific
finding is gone *because the text changed*, rather than because the check stopped looking.

⚠ **One finished item has a test plan that was never run, and it matters more than it looks.**
TASK-141 is recorded as finished, but its three manual checks are unticked. One of them asked for
an independent reader to apply the rule to the project's own check script and report what it
found. That check was skipped — and when it was finally run months later under a different item,
it found four genuine problems in exactly that file, one of which had already been wrong from the
day it was written. **The skipped test is the one that would have caught them.** Whoever picks
this up should decide whether the item goes back to awaiting-verification or whether the later
work has already covered it; it should not simply stay as it is.

## Prototype

**None built.** The feature's own notes leaned towards building one — a mock-up of what the
command's report looks like — on the reasoning that the report *is* the whole thing a person sees,
and that getting it wrong is expensive while mocking it is cheap. The command shipped without it.

That is worth recording rather than glossing: the two open items about the report's wording
(TASK-155 and TASK-156) are both about exactly what a mock-up would have settled in advance. The
prototype decision was never formally closed, so it still reads **Pending** — which is the one
thing the process says must never be left blank.

## Next step

**Decide the prototype question and the skipped test, then the feature is ready for its review
gate.** Neither is a large job. Everything requested has been built and is working; what remains
is five small follow-ups and two loose ends in the record itself.
