---
id: FEATURE-002
generated: 2026-09-18
---

# Comment discipline — keep the source free of everything that belongs elsewhere — Status

> Auto-generated by `/feature status`. Written for project managers / non-technical stakeholders — no code jargon. Do not hand-edit; re-run the verb.

**Phase:** building

## Decisions at a glance

| State | Count |
|-------|-------|
| ✅ approved | 15 |
| ✏️ changed | 0 |
| ⏸️ deferred | 0 |
| ❌ removed | 1 |
| 💭 proposed (undecided) | 0 |

## Build progress

5 / 11 tasks done (one cancelled).

Broken down today into **EPIC-004**, two pieces of work:

- **Seed the rule into both rulebooks** (STORY-018) — TASK-140 writes it into the template every new
  project is built from; TASK-141 adopts it here, together with the measurement showing the one file
  that looks like a violation is not one.
- **Build the command** (STORY-019) — TASK-142 creates it with its two scopes; TASK-143 adds the rule
  that stops it destroying the only record of something; TASK-144 makes it run automatically when
  work is closed.

Two more tasks were added while planning and stress-testing the first, which is the planning step doing
its job rather than scope escaping: TASK-145, because the scaffolder keeps a list of which parts of the
template it must not rewrite and that list would have gone one item short; and TASK-146, because the
rule ends up written in two places that cannot point at each other, so a check now enforces that they
stay identical.

One approved decision produced no task on purpose: the decision *not* to retro-fit the rule into
projects already set up. It is recorded as a boundary on TASK-140 rather than a ticket that would
sit in the backlog forever.

## What can be tested now

**TASK-140 has been tested, and it passed.** A reader who had not been told what the rule says was
given an invented project carrying it, plus a file of four comments, and asked which should stay and
which should go. It reached the intended answer on all four — including the one designed to catch a
misworded rule: a twenty-line block it kept, saying in its own words that the length was not a
finding. It also, unprompted, spotted that two lines being deleted were the only record of what they
said and that they should be filed somewhere before removal.

The other six tasks each carry a test plan that is ready to run but has not been. Two of them matter
more than the rest: whether a newly created project actually arrives carrying the rule, and whether
the new command leaves alone the comments that must survive.

## Prototype

None yet — pending a decision. The leaning is to build a written mock-up of the command's report,
because that report is the entire thing a person interacts with, and what makes it usable is
whether each finding shows which rule caught it and what would happen to it before you agree.

## Next step

`/tasks pick TASK-143` — the safety rule that stops the command destroying the only record of something.

The command now exists and works. Pointed at this very project it found four real problems in a file
this project had already declared clean — including one in a check written the same morning. It was
right, and the declaration was made by counting lines rather than by asking the question the rule
actually asks.
