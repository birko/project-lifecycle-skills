---
id: FEATURE-002
generated: 2026-09-24
---

# Comment discipline in agent-written code — Status

> Auto-generated by `/feature status`. Written for project managers / non-technical stakeholders — no code jargon. Do not hand-edit; re-run the verb.

**Phase:** review — **signed off 2026-09-24**; waiting only on one check that cannot run yet

## Decisions at a glance

| State | Count |
|-------|-------|
| ✅ approved | 14 |
| ✏️ changed | 2 |
| ⏸️ deferred | 0 |
| ❌ removed | 1 |
| 💭 proposed (undecided) | 0 |

The two changed decisions record where the build honestly departed from what was first agreed. One is
the checking command, which was agreed with two ways of choosing what to look at and shipped with three.
The other is the measurement of this project's own scripts. It was approved as "record that the main
script already follows the rule". When the measurement was actually taken, the script did **not** follow
the rule, so it was fixed rather than excused. The measurement then grew to cover all six scripts, with a
clear definition of passing: nothing that two independent readers both flag.

## Build progress

28 / 29 tasks done. One was cancelled: its work shipped inside another task, and its last part stopped
being possible when a later change removed what it applied to.

Everything asked for is built and in use:

- **The rule** is in the template every new project receives, word for word. This project follows it too.
  An automatic check keeps the two copies of the wording identical.
- **The command** checks the comments in a change, in files you name, or across the whole project. For
  each comment it reports where that comment's content already lives. It never deletes something that
  exists nowhere else: it offers to move it first, and asks. It runs automatically, as its own separate
  verdict, whenever a piece of work is closed.
- **This project's own scripts** pass the rule: two independent readers checked all six and found nothing
  they both flagged.

The review found five things unfinished, all now done. The command had not been added to the places
that list the project's tools. The text describing the closing checks still counted one check too few.
The measurement did not say how to repeat it. Content that belongs in the project's rulebook had nowhere
to be moved. And one wording mistake made during those fixes pointed a move at the wrong file.

## What can be tested now

Nothing is waiting on a person. Every task's manual checks have been run, or were recorded as not needed
with a reason.

## Prototype

**Skipped, by decision.** A mock-up of the command's report was considered. Instead, the report itself was
tested directly: independent readers ran the command on sample projects, and each run checked what a
mock-up would have shown — that every finding says which part of the rule caught it and what it would do.

## Next step

**Nothing for the stakeholder.** The feature is signed off. It becomes "done" once the project generates
its first behaviour specifications (TASK-080). One final check confirms the feature's behaviour appears
there, and that check cannot run until those specifications exist.
